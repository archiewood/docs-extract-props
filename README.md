# Extract Props from Docs

## Usage

This expects a file called `docs.txt` in the root of the project.

```
python extract.py
```

It will output a file called `props.json` in the root of the project.

